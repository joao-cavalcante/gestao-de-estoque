package wms.core

/**
 * Módulo compartilhado (Kotlin Multiplatform) para as regras de negócio do
 * domínio: resolução de código de barras, cubagem/formação de volumes,
 * validações de conferência. Compilado para JVM (backend) e, na Fase 4,
 * Android (coletor) — mesma lógica, sem duplicação entre as duas pontas.
 *
 * Ainda vazio: conteúdo real entra na Fase 1 do roteiro.
 */
const val CORE_MODULE_VERSION = "0.1.0"
